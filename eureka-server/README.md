# Eureka Server classroom scaffold

This standalone service is deliberately not part of the root Maven reactor or Docker Compose stack.

Classroom work remains to:

- Enable the Eureka server in the application class.
- Choose and configure its port.
- Configure standalone server behavior.
- Add Eureka clients to the business services.
- Replace fixed URLs with logical service names where appropriate.
