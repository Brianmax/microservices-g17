# API Gateway classroom scaffold

This standalone service is deliberately not part of the root Maven reactor or Docker Compose stack.

Classroom work remains to:

- Choose and configure its port.
- Define routes to the business services.
- Add request IDs and authentication forwarding.
- Later replace fixed route URLs with discovered service names.
